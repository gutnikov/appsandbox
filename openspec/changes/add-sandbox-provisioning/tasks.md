## 1. Спайк: реестр образов сэндбоксов

- [ ] 1.1 Проверить, можно ли из GitHub Actions сделать опубликованный в GHCR пакет публичным токеном самого запуска: собрать пробный образ в тестовом публичном репозитории, попытаться переключить видимость пакета, затем убедиться `docker pull` без аутентификации, что образ доступен анонимно
- [ ] 1.2 Зафиксировать вывод в `design.md` (раздел Risks): либо GHCR владельца репозитория, либо наш реестр с обменом OIDC-токена на короткоживущие креды. Задача считается закрытой, когда в `design.md` записано решение и его обоснование

## 2. Каркас приложения платформы

- [ ] 2.1 Инициализировать TypeScript-проект с Hono, Vite, React, TanStack Router, TanStack Query и shadcn/ui; проверка — `pnpm build` собирает бандл, `pnpm dev` поднимает приложение локально
- [ ] 2.2 Настроить отдачу собранного SPA из Hono и fallback на `index.html` для клиентских маршрутов; проверка — прямой переход на несуществующий серверу клиентский путь возвращает приложение, а не 404
- [ ] 2.3 Ввести типизированное чтение конфигурации из окружения с обязательными `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `GITHUB_OAUTH_REDIRECT_URI`, `DATABASE_URL`, `SESSION_SECRET`, `TEMPLATE_REPO`, `PUBLIC_BASE_URL`; проверка — запуск без любой из них падает с сообщением, называющим переменную (спека `platform-deployment`)
- [ ] 2.4 Добавить endpoint `/healthz`, проверяющий доступность базы; проверка — при недоступной базе endpoint не возвращает успешный статус

## 3. База и реестр имён

- [ ] 3.1 Подключить Neon и механизм миграций; проверка — миграции накатываются на чистую базу одной командой
- [ ] 3.2 Создать миграцию таблицы `sandboxes` (`name` с уникальным индексом, `github_login`, `repo_url`, `created_at`); проверка — вставка дубликата имени отклоняется базой
- [ ] 3.3 Реализовать генератор имён `sandbox-<adjective>-<noun>` на словарях; проверка — юнит-тест на соответствие формату, строчным символам и ограничению 63 символа для метки DNS (спека `sandbox-naming`)
- [ ] 3.4 Реализовать резервирование имени с повторной генерацией при конфликте уникального индекса и ограниченным числом попыток; проверка — тест, в котором первые попытки упираются в занятые имена, а результатом становится свободное имя
- [ ] 3.5 Реализовать освобождение имени; проверка — тест, что после неудачного создания репозитория имя снова доступно для резервирования

## 4. Авторизация через GitHub

- [ ] 4.1 Завести dev и prod GitHub OAuth App со скоупом `public_repo` и раздельными callback URL; проверка — реквизиты обоих приложений получены, dev-приложение указывает на локальный адрес (спека `auth/github-oauth`)
- [ ] 4.2 Реализовать роут старта авторизации: генерация одноразового `state`, сохранение его привязанным к браузеру, редирект на GitHub; проверка — редирект содержит только скоуп `public_repo` и сгенерированный `state`
- [ ] 4.3 Реализовать проверку `state` в callback с отклонением отсутствующего, несовпадающего и повторно использованного значения; проверка — тесты на все три случая, в каждом обращения к GitHub за токеном не происходит
- [ ] 4.4 Реализовать обмен `code` на токен и получение логина пользователя; проверка — тест на замоканном GitHub, что токен не попадает ни в ответ браузеру, ни в логи, ни в базу
- [ ] 4.5 Настроить куки платформы host-only с `Secure`, `HttpOnly`, `SameSite`; проверка — тест, что ни в одном ответе `Set-Cookie` нет атрибута `Domain`

## 5. Создание сэндбокса

- [ ] 5.1 Реализовать вызов порождения репозитория из шаблона в личный аккаунт пользователя с публичной видимостью; проверка — сквозной прогон на живом аккаунте создаёт публичный репозиторий, владельцем которого является пользователь (спека `sandbox-provisioning`)
- [ ] 5.2 Реализовать обработку конфликта имени на стороне GitHub: освободить имя, сгенерировать новое, повторить; проверка — тест на замоканном ответе о занятом имени, в котором пользователь получает успешный результат с другим именем
- [ ] 5.3 Записывать успешно созданный сэндбокс в реестр и связывать с адресом `<name>.zerotomvp.xyz`; проверка — после создания запись содержит имя, логин, ссылку на репозиторий и время, и не содержит токенов
- [ ] 5.4 Реализовать обработку ошибок GitHub, не устранимых повтором; проверка — тест, что пользователь получает сообщение о временной недоступности без сырого ответа API (спека `landing`)

## 6. Шаблонный репозиторий

- [ ] 6.1 Создать репозиторий `gutnikov/sandbox-template`, сделать публичным и включить флаг Template; проверка — порождение репозитория токеном со скоупом `public_repo` проходит успешно (спека `sandbox-template`)
- [ ] 6.2 Наполнить шаблон минимальным фулстек-приложением на стеке платформы; проверка — свежий клон поднимается локально командами из `README` без секретов и внешних сервисов
- [ ] 6.3 Добавить `Dockerfile`, чтение `PORT` из окружения и endpoint `/healthz`, отвечающий успешно только при готовности приложения; проверка — `docker build` и запуск с произвольным `PORT`, приложение отвечает на этом порту, `/healthz` успешен
- [ ] 6.4 Добавить workflow сборки и публикации образа с тегом по коммиту в реестр, выбранный в задаче 1.2; проверка — push в тестовом порождённом репозитории публикует образ, и платформа скачивает его без токена пользователя
- [ ] 6.5 Добавить в workflow неблокирующий шаг уведомления платформы с аутентификацией по OIDC-токену запуска; проверка — при отсутствующем или отвечающем ошибкой endpoint workflow завершается успешно и образ остаётся опубликованным
- [ ] 6.6 Написать `README` шаблона с локальным запуском и адресом сэндбокса на платформе; проверка — в шаблоне и в порождённом репозитории нет секретов платформы и не требуется настройка секретов пользователем

## 7. Интерфейс

- [ ] 7.1 Собрать лендинг с единственным действием и предупреждением о создании публичного репозитория; проверка — страница открывается анонимно, кнопка ведёт на авторизацию GitHub, ввод данных не требуется (спека `landing`)
- [ ] 7.2 Собрать страницу результата с именем, ссылкой на репозиторий и адресом поддомена, с пометкой, что сэндбокс ещё не запущен; проверка — после сквозного создания страница показывает все три значения
- [ ] 7.3 Собрать страницу ошибки для отказа в авторизации и для сбоя GitHub с возможностью повторить; проверка — отмена доступа на стороне GitHub приводит к понятному сообщению и кнопке повтора

## 8. Доставка

- [ ] 8.1 Написать `Dockerfile` платформы, собирающий SPA и запускающий сервер; проверка — образ запускается локально и отдаёт лендинг
- [ ] 8.2 Настроить публикацию образа платформы в GHCR с тегом по коммиту; проверка — образ доступен для скачивания по тегу коммита
- [ ] 8.3 Написать `config/deploy.yml` для Kamal с секретами через окружение; проверка — секретов нет в репозитории, `kamal deploy` проходит
- [ ] 8.4 Настроить DNS: A-запись апекса и wildcard `*.zerotomvp.xyz` на VPS; проверка — `dig` для апекса и для произвольного поддомена разрешаются на адрес VPS (спека `platform-deployment`)
- [ ] 8.5 Выкатить платформу на VPS с TLS и редиректом HTTP на HTTPS; проверка — апекс открывается по HTTPS с валидным сертификатом, HTTP редиректится
- [ ] 8.6 Прописать prod callback URL в prod OAuth App и накатить миграции на prod-базу; проверка — `/healthz` на prod возвращает успешный статус

## 9. Приёмка

- [ ] 9.1 Сквозной прогон на живом домене сторонним GitHub-аккаунтом: лендинг → авторизация → созданный публичный репозиторий → страница результата с адресом сэндбокса; проверка — запись в реестре появилась, репозиторий существует, экран согласия GitHub не упоминает приватные репозитории
- [ ] 9.2 Проверить, что после создания репозитория платформа не вносит в него изменений: вебхуков и секретов от платформы нет; проверка — настройки порождённого репозитория осмотрены вручную (спека `sandbox-provisioning`)
- [ ] 9.3 Проверить отсутствие токена пользователя в логах и базе после успешного и неуспешного прогона; проверка — поиск по логам prod и по таблице `sandboxes` не находит значений токена
