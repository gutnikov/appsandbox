## 1. Спайк: реестр образов сэндбоксов

- [x] 1.1 Проверить, может ли workflow сделать опубликованный в GHCR пакет публичным. Результат: нет — пакет личного аккаунта всегда создаётся приватным, видимость от связанного репозитория не наследуется, REST API смены видимости не существует (только UI)
- [x] 1.2 Зафиксировать вывод и решение в `design.md`. Результат: образы сэндбоксов складываются в собственный реестр платформы, право на публикацию выдаётся в обмен на OIDC-удостоверение запуска; реестр и выдача прав входят в этот change

## 2. Каркас приложения платформы

- [x] 2.1 Инициализировать TypeScript-проект с Hono, Vite, React, TanStack Router, TanStack Query и shadcn/ui; проверка — `pnpm build` собирает бандл, `pnpm dev` поднимает приложение локально
- [x] 2.2 Настроить отдачу собранного SPA из Hono и fallback на `index.html` для клиентских маршрутов; проверка — прямой переход на несуществующий серверу клиентский путь возвращает приложение, а не 404
- [x] 2.3 Ввести типизированное чтение конфигурации из окружения с обязательными `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `GITHUB_OAUTH_REDIRECT_URI`, `DATABASE_URL`, `SESSION_SECRET`, `TEMPLATE_REPO`, `PUBLIC_BASE_URL`, `REGISTRY_HOST`, `REGISTRY_TOKEN_KEY`; проверка — запуск без любой из них падает с сообщением, называющим переменную (спека `platform-deployment`)
- [x] 2.4 Добавить endpoint `/healthz`, проверяющий доступность базы; проверка — при недоступной базе endpoint не возвращает успешный статус

## 3. База и реестр имён

- [x] 3.1 Подключить Neon и механизм миграций; проверка — миграции накатываются на чистую базу одной командой (проверено на Neon: advisory-блокировка работает, `sslmode` закреплён на `verify-full`)
- [x] 3.2 Создать миграцию таблицы `sandboxes` (`name` с уникальным индексом, `github_login`, `repo_full_name`, `repo_url`, `created_at`); проверка — вставка дубликата имени отклоняется базой
- [x] 3.3 Реализовать генератор имён `sandbox-<adjective>-<noun>` на словарях; проверка — юнит-тест на соответствие формату, строчным символам и ограничению 63 символа для метки DNS (спека `sandbox-naming`)
- [x] 3.4 Реализовать резервирование имени с повторной генерацией при конфликте уникального индекса и ограниченным числом попыток; проверка — тест, в котором первые попытки упираются в занятые имена, а результатом становится свободное имя
- [x] 3.5 Реализовать освобождение имени; проверка — тест, что после неудачного создания репозитория имя снова доступно для резервирования

## 4. Авторизация через GitHub

- [ ] 4.1 Завести dev и prod GitHub OAuth App со скоупом `public_repo` и раздельными callback URL; проверка — реквизиты обоих приложений получены, dev-приложение указывает на локальный адрес (спека `auth/github-oauth`) (dev готов и прописан в `.env`; ждём prod)
- [x] 4.2 Реализовать роут старта авторизации: генерация одноразового `state`, сохранение его привязанным к браузеру, редирект на GitHub; проверка — редирект содержит только скоуп `public_repo` и сгенерированный `state`
- [x] 4.3 Реализовать проверку `state` в callback с отклонением отсутствующего, несовпадающего и повторно использованного значения; проверка — тесты на все три случая, в каждом обращения к GitHub за токеном не происходит
- [x] 4.4 Реализовать обмен `code` на токен и получение логина пользователя; проверка — тест на замоканном GitHub, что токен не попадает ни в ответ браузеру, ни в логи, ни в базу
- [x] 4.5 Настроить куки платформы host-only с `Secure`, `HttpOnly`, `SameSite`; проверка — тест, что ни в одном ответе `Set-Cookie` нет атрибута `Domain`

## 5. Создание сэндбокса

- [x] 5.1 Реализовать вызов порождения репозитория из шаблона в личный аккаунт пользователя с публичной видимостью; проверка — сквозной прогон на живом аккаунте создаёт публичный репозиторий, владельцем которого является пользователь (спека `sandbox-provisioning`) (создан `gutnikov/sandbox-misty-raven`, оставлен как подопытный для задач 6.5 и 7.5)
- [x] 5.2 Реализовать обработку конфликта имени на стороне GitHub: освободить имя, сгенерировать новое, повторить; проверка — тест на замоканном ответе о занятом имени, в котором пользователь получает успешный результат с другим именем
- [x] 5.3 Записывать успешно созданный сэндбокс в реестр имён вместе с полным именем репозитория, нужным для сверки OIDC-удостоверений; проверка — после создания запись содержит имя, логин, `repo_full_name`, ссылку и время, и не содержит токенов
- [x] 5.4 Реализовать обработку ошибок GitHub, не устранимых повтором; проверка — тест, что пользователь получает сообщение о временной недоступности без сырого ответа API (спека `landing`)

## 6. Реестр образов и выдача прав

- [x] 6.1 Поднять на VPS реестр образов с делегированной аутентификацией, доверяющий токенам, подписанным платформой; проверка — анонимное обращение к реестру отклоняется, содержимое не раскрывается (спека `sandbox-image-registry`)
- [x] 6.2 Реализовать проверку OIDC-удостоверения GitHub Actions: загрузка и кеширование ключей провайдера, проверка подписи, издателя, срока действия и аудитории; проверка — тесты на корректное удостоверение и на каждый вид некорректного
- [x] 6.3 Реализовать endpoint выдачи прав: сверить репозиторий из удостоверения с записью о сэндбоксе, включая совпадение владельца, и выдать короткоживущий токен только на запись в свой образ; проверка — тесты на чужой образ, на отсутствующий сэндбокс и на несовпадение владельца — во всех случаях отказ
- [x] 6.4 Проверить срок жизни и область действия выдаваемого токена; проверка — тест, что просроченный токен отклоняется реестром и что токеном нельзя прочитать чужой образ
- [ ] 6.5 Проверить приём образа сквозняком: собрать образ и запушить в реестр, предъявив настоящее удостоверение из тестового запуска Actions; проверка — образ появляется в реестре и скачивается платформой

## 7. Шаблонный репозиторий

- [x] 7.1 Создать репозиторий `gutnikov/sandbox-template`, сделать публичным и включить флаг Template; проверка — порождение репозитория токеном со скоупом `public_repo` проходит успешно (спека `sandbox-template`)
- [x] 7.2 Наполнить шаблон минимальным фулстек-приложением на стеке платформы; проверка — свежий клон поднимается локально командами из `README` без секретов и внешних сервисов
- [x] 7.3 Добавить `Dockerfile`, чтение `PORT` из окружения и endpoint `/healthz`, отвечающий успешно только при готовности приложения; проверка — `docker build` и запуск с произвольным `PORT`, приложение отвечает на этом порту, `/healthz` успешен
- [ ] 7.4 Добавить workflow сборки и публикации образа в реестр платформы с тегом по коммиту, с запросом OIDC-удостоверения непосредственно перед публикацией; проверка — push в тестовом порождённом репозитории публикует образ, и в репозитории нет ни одного секрета
- [ ] 7.5 Проверить поведение при недоступном реестре; проверка — запуск падает с видимой владельцу ошибкой, а повторный запуск после восстановления реестра публикует образ без правок в репозитории
- [x] 7.6 Написать `README` шаблона с локальным запуском и адресом сэндбокса на платформе; проверка — в шаблоне и в порождённом репозитории нет секретов платформы и не требуется настройка секретов пользователем

## 8. Интерфейс

- [ ] 8.1 Собрать лендинг с единственным действием и предупреждением о создании публичного репозитория; проверка — страница открывается анонимно, кнопка ведёт на авторизацию GitHub, ввод данных не требуется (спека `landing`)
- [ ] 8.2 Собрать страницу результата с именем, ссылкой на репозиторий и адресом поддомена, с пометкой, что сэндбокс ещё не запущен; проверка — после сквозного создания страница показывает все три значения
- [ ] 8.3 Собрать страницу ошибки для отказа в авторизации и для сбоя GitHub с возможностью повторить; проверка — отмена доступа на стороне GitHub приводит к понятному сообщению и кнопке повтора

## 9. Доставка

- [x] 9.1 Настроить DNS: A-записи апекса, wildcard `*.zerotomvp.xyz` и имени реестра на VPS; проверка — `dig` для всех трёх разрешается на адрес VPS (спека `platform-deployment`)
- [ ] 9.2 Написать `Dockerfile` платформы, собирающий SPA и запускающий сервер; проверка — образ запускается локально и отдаёт лендинг
- [ ] 9.3 Настроить публикацию образа платформы в GHCR с тегом по коммиту; проверка — образ доступен для скачивания по тегу коммита
- [ ] 9.4 Написать `config/deploy.yml` для Kamal с секретами через окружение, включая ключ подписи токенов реестра; проверка — секретов нет в репозитории, `kamal deploy` проходит
- [ ] 9.5 Выкатить платформу и реестр на VPS с TLS и редиректом HTTP на HTTPS; проверка — апекс и имя реестра открываются по HTTPS с валидными сертификатами, HTTP редиректится
- [ ] 9.6 Прописать prod callback URL в prod OAuth App и накатить миграции на prod-базу; проверка — `/healthz` на prod возвращает успешный статус
- [ ] 9.7 Убедиться, что выкат платформы не прерывает работу реестра; проверка — во время `kamal deploy` реестр продолжает отвечать

## 10. Приёмка

- [ ] 10.1 Сквозной прогон на живом домене сторонним GitHub-аккаунтом: лендинг → авторизация → созданный публичный репозиторий → страница результата с адресом сэндбокса; проверка — запись в реестре имён появилась, репозиторий существует, экран согласия GitHub не упоминает приватные репозитории
- [ ] 10.2 Сквозной прогон доставки образа: push в созданный сэндбокс приводит к появлению образа в реестре платформы; проверка — образ с тегом коммита скачивается платформой
- [ ] 10.3 Проверить, что после создания репозитория платформа не вносит в него изменений: вебхуков и секретов от платформы нет; проверка — настройки порождённого репозитория осмотрены вручную (спека `sandbox-provisioning`)
- [ ] 10.4 Проверить отсутствие токена пользователя в логах и базе после успешного и неуспешного прогона; проверка — поиск по логам prod и по таблице `sandboxes` не находит значений токена
